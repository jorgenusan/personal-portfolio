/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/jorgenusan.github.io",
    images: {
        unoptimized: true,
      }
  };

export default nextConfig;
